# Production Environment Configuration
# IB-Stream Server Production Settings

# Server Configuration
PORT=8001
HOST=0.0.0.0

# TWS Connection
IB_STREAM_CLIENT_ID=2
IB_STREAM_HOST=127.0.0.1
IB_STREAM_PORTS=7497,7496,4002,4001

# Storage Configuration
IB_STREAM_STORAGE_PATH=storage
IB_STREAM_ENABLE_STORAGE=true

# v2 Storage Formats (legacy) - DISABLED for production efficiency
IB_STREAM_ENABLE_JSON=false
IB_STREAM_ENABLE_PROTOBUF=false

# v3 Storage Formats (optimized, 50%+ space reduction) - ENABLED
IB_STREAM_ENABLE_V3_JSON=true
IB_STREAM_ENABLE_V3_PROTOBUF=true

# Other storage backends
IB_STREAM_ENABLE_POSTGRES=true

# Tracked Contracts (Production)
# Format: contract_id:symbol:tick_types:buffer_hours
IB_STREAM_TRACKED_CONTRACTS="711280073:MNQ:bid_ask;last:1"

# Performance Settings
IB_STREAM_MAX_STREAMS=50
IB_STREAM_BUFFER_SIZE=100
IB_STREAM_CONNECTION_TIMEOUT=5
IB_STREAM_RECONNECT_ATTEMPTS=3

# Logging
IB_STREAM_LOG_LEVEL=INFO